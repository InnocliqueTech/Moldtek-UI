name: Deploy on Push

on:
  push:
    branches:
      - UI-Component  # Using your actual branch name

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Run deployment script
        run: C:\Users\infra\Desktop\ui_deploy.bat  # Update this path to your actual batch file location